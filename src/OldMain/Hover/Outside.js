import React from 'react';
import Button from './Button/Button';
import './Outside.css';

function Outside(){
  const names = {
    
  }

  return (
    <div className = 'buttons'>
      <Button/>
      <Button/>
      <Button/>
    </div>
  );
}

export default Outside;
